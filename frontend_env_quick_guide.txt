# Frontend 環境變數快速指南

## 配置文件位置

### Docker 構建環境（推薦，目前使用）
📁 項目根目錄的 docker-compose.yml
   位置：sealcompare/docker-compose.yml
   區段：frontend.build.args

### 開發環境（本地開發時）
📁 frontend/.env
   位置：sealcompare/frontend/.env
   （需要手動創建，複製 .env.example）

---

## 如何設定（Docker 環境）

### 方法 1：直接在 docker-compose.yml 中修改（最簡單）

1. 打開 sealcompare/docker-compose.yml
2. 找到 frontend.build.args 區段
3. 修改對應的值：
   - "true" = 顯示組件
   - "false" = 隱藏組件
4. 執行：./rebuild-frontend.sh（或 rebuild-frontend.bat）

### 方法 2：使用項目根目錄的 .env 文件

1. 在項目根目錄（sealcompare/）創建 .env 文件
2. 在 docker-compose.yml 中使用 ${變數名} 引用
3. 執行：./rebuild-frontend.sh

---

## 常用操作

修改配置 → ./rebuild-frontend.sh → 訪問 http://localhost:3000 驗證

---

詳細說明請查看：frontend/ENV_SETUP.md
